# Instalação

- Via pip (PyPI):

  ```bash
  pip install hippocampus
  ```

- Via uv (a partir do repositório):

  ```bash
  uv tool install .
  ```

- Requisitos:
  - Python >= 3.11
  - Ambiente virtual recomendado
